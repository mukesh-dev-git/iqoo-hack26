package com.limitless.codereview.ui.theme

import androidx.compose.ui.graphics.Color

// Same dark "dev-tool" palette used across the pitch materials (dashboard mockup, walkthrough,
// architecture diagram) — kept in sync deliberately so screenshots/videos of the real app match
// the submission's visual language instead of looking like a different product.

val AppBg = Color(0xFF101917)
val Surface = Color(0xFF182220)
val Surface2 = Color(0xFF1F2B28)
val Surface3 = Color(0xFF253530)
val FrameEdge = Color(0xFF303C42)

val TextHi = Color(0xFFEAF2EE)
val TextLo = Color(0xFF8FA39B)
val TextFaint = Color(0xFF5E6F68)

val Accent = Color(0xFF3FA187)      // jade — primary brand / on-device signal
val AccentSoft = Color(0x293FA187)  // ~16% alpha

val Brass = Color(0xFFC99A4D)       // secondary accent — samples, escalation, "at event"
val BrassSoft = Color(0x29C99A4D)

val SevBug = Color(0xFFE2574C)
val SevBugSoft = Color(0x21E2574C)
val SevInfo = Color(0xFF6E93A8)
val SevInfoSoft = Color(0x216E93A8)

val DiffAdd = Color(0x293FA187)
val DiffAddText = Color(0xFF7FD9BB)
val DiffDel = Color(0x24E2574C)
val DiffDelText = Color(0xFFF0958C)
